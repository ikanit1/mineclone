---
tags: [plan, ui]
status: in-progress
date: 2026-09-15
spec: docs/superpowers/specs/2026-09-15-menu-and-performance-design.md
---

# План: часть B — главное меню в `com.mineclone.ui`

Часть A спеки закоммичена в `16af655`. Этот план — только часть B. Шаги идут
так, чтобы меню работало на каждом коммите: новый пакет строится рядом,
экраны переезжают группами, старый код `Hud` удаляется, когда его больше
никто не зовёт.

Проверка каждого шага: `.\run-tests.ps1` зелёный, сборка `out` без ошибок,
`tools\RenderHudPreview.java` рендерит новые кадры без GL-ошибок.

## Решения, принятые по ходу (в спеке их не было)

- **Язык меню — русский.** Шрифт с `Font.RANGES` уже пакует кириллицу,
  тосты и этапы загрузки уже русские. Английские подписи старого меню уходят
  вместе с ним. В инструментах предпросмотра кириллических литералов нет:
  лаунчер читает исходник в системной кодировке — строки берутся из
  скомпилированных экранов.
- **Переназначаются только клавиши клавиатуры.** Кнопки мыши (удар,
  установка, выбор блока) остаются жёсткими. F1/F3/F4/F5/F6/F11 и Esc —
  системные: захват их не принимает, иначе действие молча перекрыло бы
  отладку или выход из меню.
- **Раскладка пишется по имени действия, а не по порядку.** Добавление или
  перестановка действий не сдвигает чужие клавиши в старых настройках.
- **`LevelData` не поднимается.** Экрану создания мира нужны имя, сид и режим —
  всё это уже есть в формате v9. Дальность прорисовки — общая настройка
  (`Options`), экран создания показывает её для удобства. Версия без нового
  поля — это только риск без пользы.
- **Логотип рисуется блоками атласа** (пиксельный шрифт 5×7, верхний пиксель
  буквы — трава), а не третьим кеглем TTF: крупный кегль с кириллицей не
  помещается в атлас шрифта 1024×512.
- **Экран загрузки рисуется поверх живого фона меню**, а не полусобранного
  мира: фон уже загружен и дешёвый, а мир во время загрузки дёргается кусками.
- **«Продолжить» на титульном экране** — последний сыгранный мир одной кнопкой.

## Задачи

### 1. Клавиши: `KeyBindings`, `Input`, `Options` v5
- [x] `core/KeyBindings`: `Action` (группа, подпись, клавиша по умолчанию),
      `key/set/reset`, `conflicts()`, `isReserved`, `keyName`, запись по имени.
- [x] `Input`: `down/pressed/released(Action)`, `firstKeyPressed()`.
- [x] `Player` и `Game` читают действия вместо `GLFW_KEY_*` (движение, прыжок,
      спуск, бег, полёт, инвентарь, бросок, осмотр, консоль, слоты 1–9,
      фоторежим).
- [x] `Options.keys`, `OPTIONS_VERSION = 5`; v1–v4 читаются с раскладкой по
      умолчанию.
- [x] Тесты: конфликты, резервные клавиши, имена, запись по имени, чтение
      файла v4, круговая запись v5.
- [x] Попутно: `SaveManager.renameWorld` терял сытость — починить и закрепить тестом.

### 2. Ядро пакета `ui`
- [ ] `UiInput` (снимок ввода кадра, без GLFW), `MenuAction`, `Screen`,
      `ScreenStack` (push/back/reset, фейды, Esc).
- [ ] `TextField` (редактирование без GL: ввод, Backspace с повтором, вставка,
      предел длины, фильтр), `ScrollState`.
- [ ] `MenuTheme`: панель-стекло, кнопка (пружина наведения, звук), основная и
      опасная кнопки, слайдер, переключатель, сегменты, поле ввода, прокрутка
      с ножницами, прогресс, спиннер, отложенный текст, модальное затемнение.
- [ ] `BlockLogo`: «MINECLONE» блоками.
- [ ] Тесты: стек и фейды, `TextField`.

### 3. Титул, пауза, смерть, загрузка
- [ ] `TitleScreen` (логотип, сплэш, «Продолжить», «Одиночная игра»,
      «Настройки», «Выход»), `PauseScreen`, `DeathScreen`, `LoadingScreen`
      (этапы `LoadStage` с долями, имя мира, советы).
- [ ] `Game`: стек меню в состояниях MENU/LOADING/PAUSED/DEAD; до задачи 4–5
      список миров и настройки открываются старым кодом.
- [ ] Меню и загрузка получают стекло (`Backdrop`) над фоном.
- [ ] Кадры предпросмотра: титул, пауза, смерть, загрузка на трёх этапах.

### 4. Настройки и назначение клавиш
- [ ] `SettingsModel`, `SettingsScreen` (вкладки «Графика», «Управление»,
      «Звук»), `KeybindScreen` (группы, захват, конфликты, «Сбросить»).
- [ ] `Game.applySettings` — побочные эффекты (VSync, полный экран, карта
      теней, громкости), запись `Options` при выходе.
- [ ] Удалить `drawSettingsHub/Video/Controls/Audio`, слайдеры `Hud`,
      `SettingsTab` в `Game`.
- [ ] Кадры: три вкладки, клавиши с конфликтом и захватом.

### 5. Миры: список и создание
- [ ] `SaveManager`: `WorldInfo` с режимом, днём, размером; `duplicateWorld`,
      `uniqueWorldId`, `saveIconAsync/loadIcon`.
- [ ] `WorldSettings` (разбор сида, имя по умолчанию), `WorldSelectScreen`
      (превью, двойной клик, переименование, копия, подтверждение удаления),
      `WorldCreateScreen`, `WorldIconCache`.
- [ ] Превью мира: `render/Thumbnail` снимает кадр из резольва `Backdrop` после
      сохранения, пишет `saves/<id>/icon.png` в фоне.
- [ ] Удалить `stoneButton/stoneMenu/drawTitle/drawConfirm/drawWorldSelect/
      drawRenameDialog/drawMainMenu/drawPauseMenu/drawLoading/drawDeathScreen`,
      `Hud.MenuAction`, `Hud.WorldSelectAction`, флаги меню в `Game`.
- [ ] Тесты: сид, имя по умолчанию, копия мира, превью, `WorldInfo`.
- [ ] Кадры: список с превью, подтверждение, переименование, создание.

### 6. Живой фон и звук
- [ ] `render/SkyPalette` — палитра кадра одной функцией для игры и меню
      (бит в бит та же математика, что была в `Game.render`).
- [ ] `MenuBackground`: сутки за 3 минуты (ночь вдвое быстрее дня), погода
      `Weather`, фаза луны, сияние; купол и спрайты неба, вода.
- [ ] Музыка меню через `AdaptiveMusic`, звук наведения и клика.
- [ ] Тест цикла суток фона.

### 7. Документация
- [ ] ADR `knowledge/decisions/menu-architecture.md`.
- [ ] `knowledge/decisions/chunk-loading-strategy.md` — этапы загрузки.
- [ ] `CLAUDE.md`: пакет `ui`, клавиши, ручки настройки.
- [ ] Спека: `status: done`.
