---
tags: [plan, audio, music]
status: in-progress
date: 2026-09-15
spec: docs/superpowers/specs/2026-09-15-music-design.md
---

# План: музыка в меню и в мире

Формат — как у соседних планов проекта: задачи-чеклисты с точными файлами и
сигнатурами. Код пишется прямо в файлы по задачам, каждая задача заканчивается
зелёным `.\run-tests.ps1` и коммитом только своих файлов.

Проверка каждого шага: `.\run-tests.ps1` зелёный. Финал — автопилот в
настоящем окне.

## Решения, принятые по ходу (в спеке их не было)

- **Поправки громкости посчитаны по LUFS до написания каталога.** Замер:
  Deep Pressure −15,5, Fading into Warmth −15,0, Svetloe Pianino −14,9,
  Walking over −14,5, Warm Moog Entry −13,8, Weightless Lullaby −13,7,
  Nocturnal Drift −13,1. По RMS порядок был другим: басовый Deep Pressure
  казался громче, чем слышится.
- **Плеер отвечает главному потоку по поколениям.** `play()` сразу помнит
  запрошенный трек и номер поколения; поток музыки сообщает только «поколение N
  доиграло». Иначе режиссёр в кадре после `play()` видел бы пустой снимок
  (поток ещё не принял команду) и считал трек доигравшим.
- **Пробуждение гасит момент «рассвет» в том же кадре.** Скачок времени — это
  пробуждение, а не рассвет: иначе после неудачного жребия пробуждения рассвет
  бросал бы второй.
- **Флаги ситуации не копятся на паузе.** Иначе пауза в пещере через 15 с
  запускала бы пещерный трек в меню паузы.
- **Тест заката — 300 независимых закатов, а не 200 суток подряд.** Он меряет
  ровно то, что сказано в спеке: долю закатов, заставших тишину.

## Задачи

### 1. JLayer и файлы
- [ ] `run.ps1`: `Get-Jar 'com.googlecode.soundlibs' 'jlayer' '1.0.1.4'`.
- [ ] `build.gradle`: `implementation 'com.googlecode.soundlibs:jlayer:1.0.1.4'`.
- [ ] MP3 → `assets/music/`.
- [ ] `tools/AnalyzeMusic.java` — LUFS (BS.1770), пик, центроид, бас, темп, поправка.
- [ ] Коммит: jar, MP3, run.ps1, build.gradle, инструмент.

### 2. Каталог и ситуация (`com.mineclone.audio`)
- [ ] `MusicMood` — 11 настроений.
- [ ] `MusicTrack(id, path, moods, gainDb)`, `has(mood)`, `gain()`.
- [ ] `MusicLibrary`: `CATALOG`, `UNKNOWN_MOODS`, `scan(File)`, `track(id, path)`,
      `tracks()`, `byId()`, `isEmpty()`.
- [ ] `MusicSituation` (record): `Scene`, `menu()`, `dayPart(gameTime)`, `home()`,
      `prefer()`, `tolerate()`, `neighbours()`.
- [ ] `MusicTests` + регистрация в `TestMain`: части суток, каталог (файлы есть,
      у каждого настроения трек, поправки ≤ 0), неизвестный трек.

### 3. Режиссёр
- [ ] `MusicDirector` с `Playback`, `Output`, `Moment`, константами спеки,
      `update()`, `onWake()`, `requestNext()`, `status(position)`.
- [ ] Тесты на фейковом плеере: меню не молчит, доля музыки в мире, без повторов,
      пулы, бой, смерть и возрождение, меню ↔ мир, закат, кулдаун, несовпадение,
      пробуждение, ползунок на нуле.

### 4. Декодер и плеер
- [ ] `Mp3Stream`: `channels()`, `sampleRate()`, `read(dst, off, len)` (−1 в конце).
- [ ] `MusicPlayer implements Playback, Output, AutoCloseable`: поток
      `mineclone-music`, 6 буферов × 0,25 с, поколения, фейды, приглушение,
      фильтр EFX, `setVolume`, `position()`, `close()`.
- [ ] Тест: `Mp3Stream` на настоящем файле — формат, куски любой длины дают тот
      же PCM, конец трека.

### 5. Ситуация из игры (`com.mineclone.game.MusicSense`)
- [ ] `sample(dt, scene, paused, world, player, mobs, gameTime)`, `onBlockPlaced()`,
      `reset()`, статические `isUnderground`, `isSheltered`, `threatens`,
      `solidAbove`, `roofDistance`.
- [ ] Тесты: пробы столба, стройка и путь, опасность.

### 6. Игра
- [ ] `Game`: убрать процедурные ноты; `MusicPlayer`/`MusicDirector`/`MusicSense`;
      `updateMusic(dt)` каждый кадр; хуки установки блока, сна, `reset()` на входе
      в мир, возрождении и `/tp`; громкость; `close()` до `sound.destroy()`.
- [ ] `Hud.drawDebug` — строка `Music:`; `/music [next]` и строка в `/help`.
- [ ] Удалить `AdaptiveMusic`, `FeatureTests.testAdaptiveMusic`, `SoundEngine.playMusic`
      (файлы с чужими правками — без коммита).
- [ ] Коммит игры.

### 7. Автопилот и документы
- [ ] `Autopilot.Driver`: `musicAvailable()`, `musicTrack()`, `musicPosition()`;
      шаги «музыка меню играет и идёт» и «после выхода из мира снова трек меню».
- [ ] Прогон автопилота в настоящем окне.
- [ ] ADR `knowledge/decisions/music.md`, CLAUDE.md: раздел «Музыка», ручки.
- [ ] План и спека — `status: done`, коммит.
