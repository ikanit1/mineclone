# Выполнение ROADMAP 1.1

План: [ROADMAP_1_1.md](ROADMAP_1_1.md). Старт: 24 сентября 2026.
Статусы ниже отражают выполненные проверки, а не заранее отмеченные критерии
из таблицы milestones. M0 пока открыт; M1–M5 не завершены.

## Продолжение после паузы — 24 сентября 2026

Работа продолжена в ветке `claude/jolly-franklin-wiwdbh`: она начинается с
незавершённого снимка `bf1c49e` (`codex/m1-core-architecture`) и слияния с
`develop-1.1` (`1ce9708`), так что в ней и M0, и пауза. Снимок не компилировался:
черновик `ParticipantTests` ссылался на несуществующие контракты. Теперь дерево
собирается, а SIM-02, SAVE-07 и BLK-06 закончены и проверены (таблица M1 ниже).
[PR #6](https://github.com/ikanit1/mineclone/pull/6) из `codex/m1-core-architecture`
остаётся красным: его ветка — это прежний снимок без исправлений.

Проверено здесь (Linux, JDK 21, `--release 17`): **562 headless-проверки**, настоящий
процесс выделенного сервера (новый мир, `/save`, `/stop`, повторное открытие с
бэкапом сессии, level v11 с записью игрока), `CheckSaves` по корпусу эталонов
(7 миров, 1 гость, 0 ошибок). **Не проверено в этой среде** (нет нативных
библиотек Windows и GPU): `run-net-test.ps1`, `run-server-test.ps1` с
отрисованными клиентами, автопилот меню, удалённый CI на PR. Эти ворота нужно
пройти на машине разработки перед слиянием.

Дополнение (BLK-02): окно здесь всё-таки есть — Xvfb, программный OpenGL Mesa
(llvmpipe, около 10 кадров/с) и нативные библиотеки LWJGL 3.3.6 для Linux. Автопилот
меню прошёл все проверки на сборке `5e35533` и на сборке с BLK-02 (музыка
пропущена: нет звукового устройства), `PlayerCollisionSmoke` — PASS. Скорость и
вид кадра с настоящей видеокартой так не проверяются; удалённый CI — тоже.

Сетевые ворота с настоящими играми — тоже здесь, переносами `run-net-test.ps1` и
`run-server-test.ps1` на bash (`tools/linux/`), на сборке BLK-02, то есть с
протоколом v8 впервые вне тестов: **LAN — обе стороны прошли** (вход в мир хозяина
его генератором, все виды мобов с правильными позами, площадка, блоки и снаряжение
в обе стороны); **выделенный сервер и два клиента — прошли** (правка каждого
подтверждена сервером и дошла до другого, снаряжение через сервер, чистый `/stop`,
15 чанков и журнал генерации на диске). Photon здесь не запускался.

Полный замер PERF-01/02 по-прежнему прерван: частичные результаты
([17 прогонов из 33](perf/interrupted-20260924/README.md)) не являются принятой
базовой линией и не закрывают ворота M0.

## Базовая версия

FND-01: исходная работа сохранена в `91a2e5b`; [PR #3](https://github.com/ikanit1/mineclone/pull/3)
слит в `master` (`9670110`), тег `v1.0.1-alpha` опубликован.
Продолжение — `develop-1.1`. До правок: 455 тестов, LAN, выделенный сервер,
автопилот меню — успешно. [Подробности](audits/2026-09-24-roadmap-baseline.md).

## Реализовано, проверяется в общей сборке

| Задачи | Результат | Проверка |
|---|---|---|
| NET-05 | Хозяин выбрасывает содержимое сундука/печи до удаления гостем | Регрессия сначала падала на потере 16 алмазов; теперь TCP/петля и настоящий Game с GL: 16 + 12 предметов у обеих сторон |
| SAVE-01/02 | Типизированный отказ открытия; карантин повреждённого чанка; защита будущих форматов | Ошибки заголовка/CRC, отказ сервера exit 2, байты сохранены |
| SAVE-03 | Генерация и восстановление до публикации; владение изменяемыми данными | Барьеры, первый победитель, сетевой delta; 300 чанков × 4 потока × 20 циклов |
| SAVE-04 | ZIP перед открытием/записью; ротация; восстановление копией; `/backup` | Порядок очереди, сбой бэкапа, обход путей, неизменность оригинала; 200 МиБ за 5,428 с |
| SAVE-05/06 | Level v11, chunk v7, минимальная версия читателя, именованные секции; асинхронная запись level | Неизвестные данные сохраняются за 5 циклов; запрещённая версия не перезаписывается; снимок не заимствует изменяемый инвентарь |
| SAVE-08 | Эталоны старых миров, включая записанные настоящим тегом v1.0.0-alpha | 6 миров / 10 чанков / 45 стопок; миграция копий, SHA256 исходников неизменны |
| Дополнительные регрессии M0 | Автосохранение сервера сохраняет checkpoint владельца; прицел не вызывает GL_INVALID_VALUE | Отдельный server test и SaveLifecycleSmoke: 183 кадра / 182 GPU samples, клик восстановления открывает копию |
| SIM-01 | Общие мировые часы double + монотонные тики; сохранение секции часов | Команды, HUD, сон, равенство клиента/сервера, дробная часть |
| BLK-01 | Таблица свойств BlockProps без изменения ordinal | Зафиксированы реальные 48 исходных блоков и все свойства |
| BLK-05 | 35 рецептов и 9 вариантов плавки на JSON; теги, время и количество результата | Golden прежних таблиц, все сдвиги/отражения, расход, ёмкость печи, 12 некорректных наборов данных |
| RND-01 | Неблокирующие GPU-запросы по фазам и кольцо из 3 кадров | Очередь на поддельном backend; настоящий GL: сумма фаз отличается от полной на 0,52%, overhead 0,0228 мс |
| FND-03/05 | Фильтры тестов, отчёт, BuildInfo, Gradle mineTests/serverZip | CLI/exit codes, единая версия, запуск сервера из архива и сохранение после `/stop` |
| FND-06 | Краткий CLAUDE, архитектура, карта проекта, tools/tuning, архив истории | CLAUDE 7 КБ, ссылки проверены; прежние планы помечены superseded, не выполненными |
| FND-02 | Уборка корня и правила переводов строк; старые артефакты сохранены вне исходников | В корне нет `.class`/логов/генераторов; `git diff --check` без предупреждений LF/CRLF |
| SAVE-03, дополнительное ревью | Быстрый возврат в выгруженный чанк видит свежую запись из очереди | Регрессия сначала падала на старом блоке; 5 проверок очереди, независимых копий, ошибок записи и защиты формата |

## M1 — реализовано и проверено

| Задачи | Результат | Проверка |
|---|---|---|
| SIM-02 | `sim.Participant`/`Participants`, `LocalParticipant`, `RemoteParticipant`; живой список у `Multiplayer`: хозяин — свой игрок и гости после рукопожатия, сервер — только гости, гость — никто. Словарь урона AC-04 (`DamageType`, `DamageSource`, `ArmorView`) | 8 проверок: ближайший без аллокаций и с детерминированной ничьёй, граница радиуса, принятая, а не интерполированная позиция гостя, окно неуязвимости, петля: вход, выход, тайм-аут, переподключение, неавторизованный актор; пакет `sim` без клиента. Мутация правила рукопожатия ловится |
| SAVE-07 | Одна `PlayerRecord` для level.dat, `players/<uuid>.dat` v2 и адаптера v7; секции vitals (насыщение, истощение, воздух в тиках), equipment ×5, effects, spawn, advancements, recipes, progress, чужие | Round-trip каждой секции, неизменяемость, повреждённые effects хранятся побайтно, нечитаемый/слишком новый гость отклоняется и не перезаписывается, M0-level без записи мигрирует с бэкапом; **эталон гостя v1, записанный настоящим тегом v1.0.1-alpha**, мигрирует в v2, v1-байты в бэкапе; v7-чекпоинт гостя не стирает экипировку/эффекты (мутация ловится); автосохранение сервера сохраняет всю запись владельца |
| BLK-06 | Таблицы добычи блоков, мобов и сундуков в JSON; `LootReachability` | Golden сверен с настоящей сборкой M0 (`0b7a12a`: `Game.blockDrop`, `MobType.drop/dropCount` через рефлексию) — 48 блоков и 11 мобов совпали; ворота инструмента, ни одного случайного числа у постоянной добычи, детерминизм сундука, распределение весов на 10 000 бросков, условия, `set_damage`, 16 битых наборов данных; мёртвые рецепты — только медь (GEN-08) |
| Находка BLK-06 | Спальник не собирался в выживании с момента появления рецепта (`31afedb`): листва не выпадала. Теперь листва выпадает с шансом 1/4 | Отдельный тест доли 1/4 и единственное оговорённое отличие от golden M0 |
| GEN-01 | `world/gen`: `WorldGenVersion` (V1, V2), `GenFeatures` с постоянными битами, `GenPolicy` на каждый чанк, секция `worldgen` (версия, записанные изменения 1.1, `upgradedAt`); игра, сервер и дельта сети генерируют политикой мира. Отладочный профиль переименован в `GenProfile` | **Golden 120 чанков** (три сида, отрицательные и дальние координаты, первый чанк каждого биома, каждая постройка) записан `tools/MakeWorldGenGolden.java`; его вывод на сборке тега v1.0.0-alpha побайтно совпал — нынешний генератор и есть генератор 1.0. Правка одной линзы гравия роняет все 120; снятая проверка изменений 1.1 ловится. Незнакомая версия или неосуществлённое изменение — `TooNew`, V1 с изменением — `Unreadable`, файл не перезаписывается; новый мир сервера записывает свою версию |

| SIM-03 | `sim.WorldSession.tickMobs` — один цикл мобов у хозяина и на выделенном сервере; `sim.EntityStore`, `sim.WorldEvents` (показ) и `WorldSession.Host` (временный шов: фокус, удар, взрыв, добыча, стрелы); `game.EntityPresentation` — звуки, частицы, следы; расталкивание без `IdentityHashMap` на кадр. `Game` −99 строк, сервер −34 | **Тест паритета записан до переноса** (`79318ba`: переписанный `Game.updateMobs` без звука): сид 20260922, ночь, 9 мобов, 600 тиков — хэш позиций, здоровья, состояний, ударов, взрыва, добычи и стрел; сессия его повторяет. Двойное расталкивание, отказ выталкивать из игрока и смена интервала чувств ловятся; тот же прогон с записывающим `WorldEvents` даёт тот же хэш; в исходнике сервера нет правил мобов. Настоящий процесс сервера: 20 с с ночью, `/save`, `/stop`, выход 0 |
| Находка SIM-03 → SIM-04 | На выделенном сервере стрелы гостей (`shootFor`) добавлялись в список, но не летели и не удалялись: висели в воздухе у всех, а пакет `S_PROJECTILES` рос с каждым выстрелом | Закрыта SIM-04: тест «стрела гостя на сервере летит и падает» сначала падал |
| Находка: падение сервера | `DedicatedServer.tickItems` передавал `ItemEntity.update` пустую цель магнита, и первый же предмет на земле (бросок гостя, разбитый сундук, песок на факеле) выбрасывал `NullPointerException` из тика: цикл сервера кончался без `shutdown`, мир с последнего автосохранения не записывался | Отдельный коммит `701706f`: регрессионный тест сначала падал этим исключением. Предложение (не сделано): тик, упавший с исключением, всё равно должен сохранить мир и остановить сервер |
| SIM-04 | В `WorldSession` — предметы (физика, слияние, потолок, подбор своим игроком), стрелы, взрывы (`explode`: воронка, урон участникам через `Participant.damage`, мобам), добыча (`dropLoot`), запись сущностей чанка (`snapshotChunk`) — одним кодом у хозяина и сервера. Убийца помнится номером (`Mob.killer`): удар хозяина, `C_MOB_HIT` гостя, владелец стрелы. Правило «креатив хозяина — без добычи» заменено на «убийца в креативе — без добычи». `DedicatedServer.tickItems/itemsInChunk` удалены | **Тест паритета снова записан до переноса** (`a8e5fd7`, переписанный `Game.detonate`, хэшируется и каждая изменённая мировая клетка: воронка 41 блок); сессия его повторяет. Настоящий тик сервера: предмет из сохранённого чанка переживает сессию (**сервер стирал его первой записью**), стрела гостя летит, крипер делает воронку и ранит гостя по петле, корова, убитая гостем, роняет говядину у сервера и у гостя, убийство гостем в креативе — ничего. Четыре из этих тестов падали до переноса. Мутации «убийца забыт», «нет крючка живого чанка», «взрыв мимо участников» ловятся. Настоящий процесс сервера: ночь, `/save`, `/stop`, выход 0 |

| SIM-07 | `sim.TargetSelector`: цель моба среди живых участников не в креативе — ближайший видимый, удержание (другой видимый ближе на 30 % или цель не видна 3 с), месть ударившему 20 с; память на мобе. Удар — `Participant.damage` цели (`MELEE`), своему игроку отброс и эффект элитника через `Host.participantStruck`, гостю — `S_PLAYER_HURT`. Шум от блока гостя слышат мобы хозяина и сервера. `Host.hostileMobs/mobStruck` удалены | **Тест паритета не сдвинулся** (один кандидат — всегда цель). `TargetSelectionTests`: ближайший видимый, креатив и мёртвые, потеря видимости за стеной, запас 30 %, месть, одинокий кандидат; ошибка «смена на ближнего за стеной» найдена этим тестом и исправлена. Сервер: зомби бьёт гостя по петле (раньше удар не доходил ни до кого — мутация «удары выключены» ловится), зомби слышит сломанный гостем блок. Настоящий процесс сервера: ночь, `/save`, `/stop`, выход 0 |

| SIM-05 | `WorldSession.centres()` — все участники (или точка появления в пустом мире); тики блоков и печи по объединению квадратов (каждый чанк один раз, в прежнем порядке), спавн кольцом вокруг каждого с местным капом (128 блоков) и общим ×min(4, n), деспавн по ближайшему; сервер получает осадки фронта (`WorldClock.frontSeconds`). `DedicatedServer.centre()` удалён | Тест паритета не сдвинулся. `WorldAroundEveryoneTests`: печь у второго игрока в 300 блоках плавит, а ничья — нет; двое в одном чанке тикают его ровно как один; мобы появляются у обоих, корова у второго не исчезает из-за дальности от первого; осадки сервера равны фронту. Мутации «чанк тикается дважды», «печи только у первого», «деспавн от первого» ловятся. Настоящий процесс сервера — выход 0 |

| GEN-02 (мир) | `world/gen/ChunkLedger` — версия, которой сделан каждый чанк; игра и сервер генерируют политикой журнала (`WorldGenSettings.policy(ledger)`): известный чанк держит свою версию, новый пишется с версией мира. Файл `chunks/ledger.dat` (gzip, отсортированные дельты ключей VarLong, версии RLE), одна атомарная перезапись вместо `ledger.log` из плана. `WorldGenUpgrade`: закрепить виденное (сохранённые чанки, кольцо 12, спавн и игрок) и поднять версию; `seams` и `tools/SeamReport.java`. Повреждённый журнал уходит в `ledger.dat.corrupt-*` после бэкапа, а без бэкапа остаётся на месте и не перезаписывается; мир открывается всегда | **Приёмка: эталонный мир 1.0 `alpha-small` после обновления до V2** — 15 чанков (каждый сохранённый и в 12 от него) по хэшу равны генерации V1, земля в 200 чанках получает V2; журнал переживает запись. Порча (9 видов), очередь и чтение до записи, бэкап несёт журнал, защищённый мир не пишется, восстановление консервативно (включая якорь). Мутации «не записывать новый чанк», «без проверки порядка ключей», «без восстановления», «кольцо 11» ловятся. Кодировщик переписан без блокировки на ключ: побайтно тот же вывод, 100 000 чанков 19 → 8 мс. Настоящий процесс сервера: журнал 169 чанков записан, перечитан после перезапуска без перезаписи, лежит в бэкапе сессии |

| SURV-01 | Один путь урона: `Damageable.damage(DamageSource, amount)` у `Player`, `Mob`, `RemotePlayer`, участников; `Hittable` его наследует. `DamageSource` + конечность и флаг `PLAYER` (удар игрока, даже если номер стрелка потерян), фабрики `byPlayer/byMob/of/onLimb`; `ArmorMath` — место брони (CMB-02). Игрок: окно для атак, среда без окна, креатив неуязвим кроме `VOID`, `lastDamageSource`. Падение, голод, огонь, яд, удары, стрелы, взрывы, укусы, `C_MOB_HIT` и v7 `S_PLAYER_HURT` — через новый путь; `takeDamage/takeAttackDamage/hurt*/takeProjectile` — адаптеры | **Тест паритета не сдвинулся.** `DamageTests` (14): окно и реген, креатив и пустота, причина смерти до возрождения, битые числа, пол голода ровно, адаптеры, окно моба и среда, удар в окне не ранит конечность, кто убил — что осталось (игрок, волк, взрыв, огонь, стрела ушедшего), злость и месть, отброс по `knockback`, стрела называет стрелка, броня не трогает утопление/голод/яд, **сканер: в `src/main` нет вызовов старых входов**. Десять мутаций ловятся. Найдено по дороге: удар в окне неуязвимости ранил конечность; `C_MOB_HIT` с NaN портил позицию моба хозяина (тест петли) |

| NET-01 | `NetStats`: байты и пакеты по коду и направлению, сообщения и доставки, полезная нагрузка по участнику; итоги и окна 1–60 с по целым секундам; запись без аллокаций. `NetChannel` засчитывает пакет при отправке сообщения (тогда известно число получателей), `Multiplayer.onPayload` — приход; `NetTransport.wireBytes`: Photon — точный размер кадра события с base64, LAN +13 Б, петля — как есть. F3: скорость и сообщения комнаты за последнюю секунду; `-Dmineclone.netStats` — CSV по секундам; `tools/NetBaseline.java` | `NetStatsTests` (6): окна и забывание минуты, топ кодов и скорость без аллокаций, CSV каждой секунды один раз, **петля хозяина и двух гостей: коды в сумме дают ровно сообщения, доставленное = принятое, 22 Б на `X_PLAYER_STATE`**, битый пакет забирает остаток, размер Photon сверен с настоящей строкой кадра; пять мутаций (широковещание засчитано один раз, потерянный остаток, окно с текущей секундой, несброшенная ячейка, длина кадра Photon) ловятся. **Базовая линия v7** (`docs/perf/net-baseline.md`): типичная сцена (3 гостя, 30 мобов, 50 предметов) — 37,9 КБ/с на гостя (51,3 на проводе Photon), 240 сообщений комнаты/с; мобы 85 Б/моб, предметы 39 Б/шт. Находки для NET-03: конверт Photon утраивает мелкий трафик; гость шлёт полную контрольную точку 4 раза в секунду без изменений (854 Б с полным инвентарём ≈ 3,3 КБ/с). Сцена `mp-host-3` с окном здесь не запускалась |

| NET-02 (часть 1) | `NetProto.VERSION = 8` на первом несовместимом теле: `S_PLAYER_HURT` — `PlayerHurt` (сумма, вид урона по постоянному id `DamageType`, флаги игрока и точки, точка, отброс, вид моба, номер участника), поля сырые до `valid()`. Гость применяет урон своим путём и отлетает от удара и взрыва, как игрок хозяина. Отказ по версии — `ConnectDiagnosis.versionMismatch`: обе версии, сборка хозяина, кому обновляться (сессия и кадр LAN). `docs/NETWORK_PROTOCOL.md` — все 36 кодов. `MobSnapshot.valid()` без аллокаций (TD-35) | `ProtocolTests` (6): документ = коды `NetProto` (и версия), круговой путь каждого вида источника, обрезанное тело — `truncated()`, 16 битых наборов отвергнуты и на проводе, текст отказа, проверка снимка без аллокаций. Петля: вид, моб и точка удара доходят до гостя целиком; обрезанный и битый пакет урона ничего не меняют. Пять мутаций ловятся |

| NET-02 (часть 2) + GEN-02 (сеть) | `S_GEN_MAP (74)` — чанки на другой версии, чем у мира (`GenPolicy.pinned()`), кодировкой журнала в gzip, перед `S_WELCOME`; `S_WELCOME` v8 (`Welcome`) — генератор хозяина и точные часы (double-время и тики); `S_CHUNK_DELTA` — версия, против которой снята дельта. Гость строит `LedgerPolicy(настройки хозяина, закреплённые чанки)`; несовпадение версии в дельте — закрепить версию хозяина (`ChunkLedger.pin`) и пересобрать чанк по её генерации. Именованные политики `GenPolicy.Fixed` и `LedgerPolicy`; `NetContext.startRemoteWorld(RemoteWorld)`. Растяжка GEN-01 заменена правилом «V2 — только с первым изменением» | `ProtocolTests`: карта (пустая, 2 026 чанков < 1 КБ, обрезанная, не gzip, неизвестная версия, бомба), приветствие (круговой путь с точным double, обрезки, неизвестный генератор, 5 битых наборов). Петля: **обновлённый хозяин с закреплённой землёй — у гостя те же версии и те же блоки**; хозяин, «забывший» чанк в карте, — гость закрепляет его по дельте и получает правку; точные часы; неизвестный генератор и битая карта заканчивают сессию с причиной. Пять мутаций ловятся |

| NET-02 (часть 3) | `C_PLAYER`/`S_PLAYER` и все пакеты с контрольной точкой (`C_OPEN`, `C_DROP`, `C_PICKUP`, `S_PICKUP`) несут всю `PlayerRecord` (`PlayerRecordCodec` с её версией формата); раскладка v7 — только `PlayerData.readV7` для файлов гостей v1. Гость (`Game`) восстанавливается из записи хозяина целиком и носит её дальше; хозяин применяет точку гостя `PlayerRecord.mergeClient` (модель доверия 1.1: своё гостя — поза, жизненные показатели, предметы, экипировка, прогресс, достижения, рецепты; эффекты, личный спавн и чужие секции — хозяина). `mergeLegacy` удалён | Петля: запись с экипировкой, эффектами, спавном и секцией новой сборки доходит до гостя целиком и возвращается с его точкой; точка гостя без эффектов, спавна и секции их не стирает, а достижения гостя берутся. Круговой путь записи, обрезки, запись новее сборки — отказ. Три мутации ловятся (четвёртая эквивалентна: обрезанное тело — пустые байты, их отвергает кодек) |

| BLK-02 | `world/shape`: `BlockShape` (коробки столкновения и прицела по meta, в массив вызывающего без аллокаций) и реестр `Shapes` с исчерпывающим switch — `FULL`, `EMPTY`, `STAIRS` (плита + ступенька), `DOOR_CLOSED`/`DOOR_OPEN`, `LAYER`, `TORCH`, `CROSS`, `FLAME`. Столкновения игрока (`resolveStairs` удалён), мобов и предметов (`EntityPhysics`), луч (`Raycaster`: DDA по клеткам, в клетке с формой — луч против коробок, нормаль грани коробки, `Hit.distance`), рамка (`Shapes.edges` — рёбра объединения коробок, у ступени Г из 18 рёбер; `OutlineAnimator` переносит их в доезжающий бокс) и мешер ступеней, дверей и слоёв читают одну форму. Лава больше не цель (TD-50); огонь остаётся целью, пока тушение по грани не придёт с BLK-03. **Автоподъёма нет**: план просит шаг ≤ 0,6, но `PlayerPhysicsTests` держит просьбу пользователя из `99a10a4` («ступень не поднимает»), а BLK-02 обязана оставить его зелёным — нужно решение владельца. `PathFinder` не менялся: без подъёма на ступень всё равно запрыгивают, пол на 0,5 появится с первой плитой (BLK-07). Закрытая дверь по-прежнему занимает клетку для тела | `ShapeTests` (9): форма каждого meta каждого блока согласна с `solid`, ступени, панели дверей (значения удалённого `doorBox`), слои, факел, **мешер рисует ровно коробки формы**, рёбра объединения, перенос рёбер при доезде, в `Player`/`EntityPhysics` нет `STAIRS`. `CollisionParityTests` (5) против переписанного старого кода: **кубы бит в бит — 1000 ходов игрока и 1000 шагов мобов и предметов**, ступени — 1000 ходов там, где старая модель по центру тела видела ту же половину (в большинстве ход касается ступени), и четыре исправленных отличия: тело в ступеньке, голова в ступени снизу, отброс к дальнему краю клетки, спринт сквозь удар. `RaycastShapeTests` (8): 1000 лучей через кубы как прежде, верх плиты и ступеньки, ступенька спереди и сзади, над плитой к блоку за ней, лава, открытый проём, факел, снег. Тест паритета сессии не сдвинулся. Девять мутаций ловятся. С окном (Xvfb + Mesa): `PlayerCollisionSmoke` PASS, автопилот меню — все проверки (включая «стена не поднимает игрока»); снимок настоящим конвейером с рамками и без них — разница ровно по Г ступени, коробке снега, панели двери и палочке факела |

| BLK-03 | `world/behavior`: `BlockBehavior` (размещение, meta, `onPlaced`, использование → `UseResult`, опора, обрушение, `onRemoved`, заготовка `scheduledTick` для BLK-04), исчерпывающий реестр `Behaviors` — сундук, печь, верстак, спальник, дверь, факел, ступени, снег. `World.setBlock` зовёт `onRemoved` уходящего блока до записи: сундук и печь высыпаются **при любом снятии** (кирка, гость, взрыв, `/fill`) в `DropSink` сессии; у зеркала гостя стока нет. `NeighbourUpdates`: клетка и шесть соседей, только блоки с опорой, бюджет 256 за тик в `WorldSimulation.update`, только где мир симулируется. `game/InteractionController` (428 строк) вместо `Game.handleInteraction` и частных методов пипетки, разлома, сторон света; `Game` 6850 → 6548 строк. Гость: `C_USE_BLOCK (70)` — хозяин проверяет досягаемость от принятой позиции и исполняет `use`; дверь гость распахивает у себя без правки-просьбы (`Multiplayer.predicted`). Особые случаи высыпания в `Game.applyRemoteBlock` и `DedicatedServer` удалены. Сон гостя больше не проматывает его часы: только точка возрождения (личный спавн записи игрока к возрождению пока не подключён). Тонкий снег уступает ставящемуся блоку | `BehaviorTests` (12): сундук при снятии воздухом, камнем, водой, и вид открытого окна очищен; печь; **взрыв настоящей сессией**; дверь целиком, одна добыча; факел на полу и на каждой стене; снег; зеркало ничего не ставит в очередь; бюджет на 512 факелах; тик мира работает с очередью только при симуляции; стороны света сверены с удалёнными методами `Game`; набор интерактивных блоков; приёмка (нет `handleInteraction`, контроллер < 600 строк). `NetworkTests`: `C_USE_BLOCK` по петле открывает дверь хозяина целиком; камень, обрезанный пакет, гость вне досягаемости — ничего. **`ContainerBreakNetworkTests` (TD-05) зелёный без частного фикса.** Тринадцать мутаций ловятся (одна — очистка слотов — сначала выжила и получила проверку). С окном (Xvfb): `CreativeGameSmoke` настоящими щелчками — прежние проверки плюс дверь целиком, открытие по панели, факел сквозь открытый проём; `ContainerBreakSmoke` — настоящие игры хозяина и гостя: сундук 16, печь 12. Сетевой автопилот ставил метку-факел в воздухе над площадкой — такой факел теперь падает, и `run-net-test` на сборке BLK-03 сначала провалился; метка-факел стоит на площадке, в стороне от игроков |
| BLK-04 | `world/ScheduledTicks` в чанке: двоичная куча `long` (срок 40 бит, вид 8, клетка 15), повторная просьба того же вида в той же клетке переносит срок, не больше 32768 на чанк. `World.scheduleTick(x, y, z, kind, delay)` (задержка ≥ 1) и `BlockBehavior.scheduledTick(..., lateBy)`; тик приходит, только если попросивший блок ещё стоит в клетке. `world/TickScheduler`: каждый тик мира с прошлого прогона по очереди (кадр на 30 тиков — три тика счётчика вовремя), 512 за тик мира, остальные на тик позже; простой длиннее 40 тиков — поздние тики, не повтор всего промежутка; чанк тикает при четырёх загруженных соседях. `WorldSession.tickScheduled()` после случайных тиков — в `Game` (только хозяин) и `DedicatedServer`. Секция чанка `ticks` (необязательная): `writtenAt`, сроки относительно него (zigzag VarLong); восстановленный чанк передаёт тики симуляции при публикации (`World.tickArrivals`), не патчем после неё. Отсутствие считается (`lateBy`), но не больше двух суток мира (`MAX_TICK_LATENESS` = 2 × `WorldClock.TICKS_PER_DAY` ≈ 50 266); часы уровня позади записи чанка — сроки сдвигаются назад; неизвестный вид держится и пишется обратно. Ни один блок пока не просит тиков — посевы и саженцы придут с ними | `ScheduledTickTests` (10): порядок кучи и равных сроков, перенос просьбы, бюджет и новая просьба обработчика, предел, удержанные виды и сдвиг назад, круг секции и шесть битых полезных нагрузок, прогон мира (длинный кадр, простой, сменённый блок, выгруженный сосед, просьба без задержки), 512 за тик по трём чанкам. **Приёмка: счётчик с интервалом 20 тикает ровно десять раз за десять интервалов через сохранение настоящим `SaveManager` и новую игру через `ChunkLoader`**; полчаса и три часа отсутствия (предел — двое суток), часы позади записи, правка до первого прогона сохраняет исходную точку. 23 мутации ловятся (две сначала выжили — просьба без задержки и исходная точка неусыновлённых тиков — и получили проверки). Полный набор 676. С окном (Xvfb): выделенный сервер с двумя настоящими клиентами на сборке BLK-04 — оба PASS, 18 чанков на диске, без исключений. Ни один блок пока не просит тиков, поэтому в настоящей игре тик по расписанию не срабатывал ни разу: проверено, что путь публикации, прогон и сохранение ничего не ломают |
| SIM-08 | Проход 1 декомпозиции `Game`: целыми методами в пакет `game`. `CommandProcessor` (все команды консоли и строки справки; узкий `CommandTarget`), `GameNetContext` (реализация `NetContext` вместо анонимного класса на 209 строк, плюс применение чужих правок, действий и контейнеров хозяина), `WeaponController` (лук, заряженный бросок, бросок стопки), `FootstepFeedback` (шаги, следы, снег из-под ноги, приземление, шаги чужих игроков, пройденный путь для покачивания), `AmbientEffects` (фоны пещеры, дождя, бури и воды, эхо, ручей и лава, рыбы, листья, светлячки, дым факелов, пар дыхания). Тела методов прежние; помощники читают поля `Game`, ставшие пакетными вместо приватных, и держат только свои таймеры. Два мёртвых поля `Game` удалены (`WATER_TICK_INTERVAL`, `BEDROLL_META`). **`Game.java` 6551 → 5456 строк** (6850 до BLK-03). План называл четыре класса; их не хватало до < 5500 — пятым стал `AmbientEffects` | `CommandProcessorTests` (6, без окна): `/time` (сутки и луна сохраняются, тики не двигаются, неверный ввод отвергается), `/weather`, `/tp`, `/spawnpoint`, `/speed`, `/fly`, `/gamemode`, `/instamine`, `/debug`, `/music`, `/help`, строка без «/» — чат только в комнате, `/fill`. Полный набор 682. С окном (Xvfb): `CreativeGameSmoke` (включая числовые `/gm`) и `InventorySafetySmoke` (6 сценариев; бросок гостя теперь через `weapons`) — PASS; LAN-пара хозяина и гостя (оба PASS) и автопилот меню (все 33 проверки) — PASS |

Серверные мобы теперь расталкиваются, стреляют и бьют гостей, а мир живёт вокруг
каждого; с протоколом v8 гость отлетает от удара и взрыва. Волчья охота сценарием
паритета не достигается. Автопилот и сетевые проверки с окном теперь проходят и
здесь (см. начало), но в их сценарии не входят подбор предметов и стрел своим
игроком, отброс взрывом, звук и частицы взрыва через `EntityPresentation` — эти
пути по-прежнему проверены только без окна.

Новые миры пока V1: у V2 нет ни одного изменения. От GEN-02 сделаны мировая и
сетевая части (гость строит мир генератором хозяина); диалог «Обновить генерацию для новых
земель?» и `upgrade-worldgen` ждут, пока V2 начнёт менять землю (сейчас они лишь
записали бы номер версии). `LATEST` может сдвинуться на V2 вместе с первым
изменением V2 (медь, GEN-08) — это проверяет `WorldGenGoldenTests`.

Следующие по плану M1: GEN-03 (каркас построек), GEN-04, GEN-08, RND-02, RND-09 ч.3; SIM-06 (фиксированный шаг, go/no-go) требует замера на 144 FPS и просмотра видео — в контейнере без GPU его не выполнить.

## В работе и оставшиеся ворота M0

- FND-04: CI [PR #4](https://github.com/ikanit1/mineclone/pull/4) зелёный (1:25 и 1:37); отдельный намеренно сломанный тест дал красный CI, проверочный PR закрыт. [Доказательства](audits/2026-09-24-ci-foundation.md).
- PERF-01/02: 11 настоящих сцен, три повтора, JSON и медианы; базовая линия ещё не готова.
- RND-09 часть 1: исходники брони, мотыг и посевов сохранены с заданиями в `assets/textures/source`; это ещё не встроенные предметы. Мотыгам при импорте нужна очистка полупрозрачного ореола.
- Общий прогон, LAN/server, автопилот и просмотр кадров прошли; новые исправления после ревью требуют повторного адресного прогона.

Проверка пользовательских миров только чтением: **4 мира, 2 874 чанка, 59 стопок,
0 отсутствующих ID, 0 ошибок** (`roadmap-real-saves-audit.log`). Автопилот с v11/v7
и GPU-таймерами прошёл все проверки открытия, меню и инвентаря; кадры просмотрены.

Локальные журналы лежат в игнорируемом `out-test/roadmap-*.log`.
Общий прогон после исправления очереди чанков: **528 passed, 0 failed, 18,832 с**
(`roadmap-m0-final-tests.log`); Gradle `test serverZip` и запуск извлечённого сервера
также прошли. LAN: обе стороны прошли; выделенный сервер и оба клиента
прошли, после `/stop` сохранены 8 чанков (`roadmap-m0-net.log`, `roadmap-m0-server.log`).
Все 11 сцен прошли короткий GL-прогон 1920×1080 (1 с прогрева + 2 с измерения),
включая проверку численности нагрузки. Это ещё не полная базовая линия.
