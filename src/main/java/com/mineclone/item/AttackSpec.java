package com.mineclone.item;

/**
 * Боевая часть предмета: урон и темп ударов.
 *
 * <p>Пока её не носит никто — оружие приходит планом B. Тип заведён сразу,
 * чтобы формат данных не пришлось поднимать из-за одного нового поля.
 */
public record AttackSpec(float damage, float speed) {}
