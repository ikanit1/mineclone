# Packed atlas is a write-only debug dump assembled at runtime from
# assets/textures/blocks/*.png — never read back, no need to version it.
assets/atlas.png

# Brainstorming visual-companion scratch (mockups, server state)
.superpowers/

# Gradle/build outputs
.gradle/
build/
out/
out-test/

# IDE compiled output
bin/
*.class

# graphify AST cache embedded in source tree (not the root graphify-out/)
src/**/graphify-out/

# Intermediate asset generation artifacts
door_bottom_tile.png
door_top_tile.png

# Local runtime state
saves/
options.dat
player-id.txt

# Preview/bench output trees written by the tools in tools/
out-net/
out-video-review*/

# Scratch logs left by local runs
*.log
run_out.txt
run_err.txt
vlc-help.txt

# Machine-local Claude Code state (absolute temp paths, one session only)
.claude/launch.json
.claude/settings.local.json
